'use strict';

angular.module('eventosEnHondurasApp')
  .controller('AddEventController',['$scope','EventService', function ($scope, EventService) {

        var init = function(){

        };

        init();
  }]);
